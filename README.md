# Hermes Config Patches

Hermes Agent configuration overrides and patches.

## Structure

```
config/
  runtime_footer.yaml   # runtime_footer config overrides
  display.yaml          # display settings overrides
  ...
README.md
```

## Usage

Copy patches from this repo into `~/.hermes/config.yaml` under the appropriate sections.
